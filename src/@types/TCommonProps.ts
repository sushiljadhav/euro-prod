export enum size {
  sm = "sm",
  md = "md",
  lg = "lg",
}

export enum color {
  default = "default",
  primary = "primary",
  secondary = "secondary",
  success = "success",
  warning = "warning",
  danger = "danger",
}

export enum radius {
  none = "none",
  sm = "sm",
  md = "md",
  lg = "lg",
  full = "full",
}

export enum placement {
  outside = "outside",
  inside = "inside",
  outsideLeft = "outside-left",
}

export enum variant {
  flat = "flat",
  faded = "faded",
  bordered = "bordered",
  underlined = "underlined",
}
